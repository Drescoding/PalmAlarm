import React, { Component } from 'react';
import { Text, TextInput, View, StyleSheet, Alert } from 'react-native';

class InputManual extends React {
  state = {
    barCodeNumber: null
  };

  render() {
    return (
      <View>
        <TextInput

        />
      </View>
    )
  }
}

extends default InputManual;
